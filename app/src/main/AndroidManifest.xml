<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".ServiceApplication"
        android:allowBackup="true"
        android:largeHeap="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NaviRotation"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">

        <activity
            android:name=".ui.map.MapActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".base.BaseActivity"/>

        <activity
            android:name=".ui.map.SearchActivity"
            android:windowSoftInputMode="adjustNothing" />

        <activity android:name=".ui.map.NavigationActivity"/>

        <!-- 수정된 CCTV 플레이어 액티비티 -->
        <activity
            android:name=".ui.map.CctvPlayerActivity"
            android:exported="false"
            android:parentActivityName=".ui.map.MapActivity">
            <!-- 업 네비게이션을 위해 parentActivity 지정 -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".ui.map.MapActivity"/>
        </activity>

        <meta-data
            android:name="com.naver.maps.map.CLIENT_ID"
            android:value="${NAVER_CLIENT_ID}"/>

    </application>
</manifest>
